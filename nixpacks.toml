[phases.setup]
# Install Python and pip before anything else
packages = ["python3", "python3Packages.pip", "libpq"]

[phases.install]
commands = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.start]
command = "bash start.sh"